machine:
  python:
    version: 3.5.2
dependencies:
  pre:
  - cp contrib/env-sample .env
test:
  override:
    - coverage run manage.py test
  post:
    - coverage html -d $CIRCLE_ARTIFACTS --fail-under 82
deployment:
  production:
    branch: master
    commands:
      - git remote add deploy dokku@104.236.104.21:tety
      - git push deploy master