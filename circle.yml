machine:
  python:
    version: 3.5.2
dependencies:
  pre:
  - cp contrib/env-sample .env
test:
  override:
    - coverage run manage.py test --fail-under 83
  post:
    - coverage html -d $CIRCLE_ARTIFACTS
deployment:
  production:
    branch: master
    commands:
      - git remote add deploy dokku@104.236.104.21:tety
      - git push deploy master